# Laravel E-commerce Template

A complete, dockerized, production-ready Laravel e-commerce template.

## Installation

```bash
git clone ...
cp src/.env.example src/.env
docker-compose up -d
```

## Access
Frontend:
```
http://localhost:8000
```